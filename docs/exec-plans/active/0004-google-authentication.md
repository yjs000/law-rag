# 실행 계획 0004: Google 인증과 계정 수명주기

상태: TODO
작성일: 2026-07-15
소유자: 미정

## 목적과 사용자 결과

현재 완성된 로그인·회원가입 UI를 실제 Supabase Google OAuth와 연결한다. 사용자는 Google 계정으로 가입하거나 로그인하고, 안전하게 세션을 복원하며, 자신의 질문 이력만 열고, 로그아웃하거나 계정 및 연결 데이터를 삭제할 수 있어야 한다.

## 범위와 비범위

범위에는 Google OAuth PKCE 시작·callback, Supabase 세션 검증, 내부 사용자 ID 매핑, 가입 동의 버전 기록, 질문 이력 RLS, 로그아웃, 계정 삭제 전파, 인증 오류·취소·만료 UI 연결과 보안 테스트가 포함된다.

이메일·비밀번호 가입, 비밀번호 재설정, Google 외 공급자, 조직·팀 계정은 비범위다.

## 구현 TODO

- [ ] 로그인과 회원가입 버튼을 Supabase Google OAuth PKCE 시작 경로에 연결
- [ ] OAuth `state`·nonce·redirect allowlist와 callback 오류 검증
- [ ] 서버가 Supabase JWT 서명·issuer·audience·만료를 검증하고 브라우저 저장소의 목업 토큰 제거
- [ ] Google 첫 로그인 시 내부 사용자 프로필을 생성하되 공급자 ID와 내부 ID를 분리
- [ ] 서비스 이용약관·개인정보 처리방침의 버전, 동의 시각, 철회 이력 저장
- [ ] 로그인·가입·취소·공급자 오류·세션 만료·재인증 상태를 현재 인증 모달에 연결
- [ ] 질문 이력·내보내기·피드백에 사용자별 RLS와 교차 사용자 접근 차단 적용
- [ ] 로그아웃 시 서버·브라우저 세션을 모두 무효화하고 다중 탭 상태 동기화
- [ ] 계정 삭제 시 질문·이력·세션·내보내기·동의 데이터 및 백업 수명주기 전파
- [ ] 익명 질문을 로그인 후 소급 저장하지 않는 계약 유지
- [ ] 정상·취소·오류·만료·재시도·교차 사용자·삭제 경계 통합 테스트
- [ ] 실제 Google OAuth와 Supabase staging 종단 테스트 후 목업 인증 경로 제거

## 완료 조건

- 공개 Preview에서 Google 가입·로그인·로그아웃·세션 복원·계정 삭제가 종단 동작한다.
- 로그인 사용자는 자신의 질문 이력만 볼 수 있고 익명 질문은 저장되지 않는다.
- 서비스 role/secret key와 원문 토큰이 브라우저·로그·분석 이벤트에 노출되지 않는다.
- 동의하지 않은 가입, 변조 callback, 만료 토큰, 다른 사용자 이력 접근이 차단된다.

## 검증과 롤백

FastAPI 인증·RLS 통합 테스트, Next.js 인증 상태 테스트, staging Google OAuth 종단 테스트를 실행한다. 장애 시 실제 인증 진입을 닫고 익명 검색·질문은 유지한다. 목업 인증은 production에서 계속 비활성 상태여야 한다.

## 결정 로그

- 2026-07-15: 로그인과 가입은 Google 단일 공급자의 같은 OAuth 경로를 사용하고, 첫 로그인 여부로 내부 계정 생성만 구분한다.
- 2026-07-15: 이번 UI 변경에서는 실제 인증 호출을 추가하지 않고 명시적인 연결 예정 상태만 제공한다.

## 진행 기록

- 2026-07-15: 로그인·회원가입·필수 동의·데이터 정책·인증 연결 예정 상태 UI를 완성했다.

## 미결정과 차단 요소

- 실제 Supabase 프로젝트·Google OAuth client·redirect URL 입력이 필요하다.
- 이용약관과 개인정보 처리방침의 최종 법률 검토 및 버전 값은 미결정이다.
